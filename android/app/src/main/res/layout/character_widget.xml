<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_name"
        android:textColor="#E65100"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_favorites_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_title"
        android:layout_marginTop="8dp"
        android:text="@string/widget_favorites_label"
        android:textColor="#6D4C41"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/widget_favorites_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_favorites_label"
        android:text="0"
        android:textColor="#E65100"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_last_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_favorites_count"
        android:layout_marginTop="4dp"
        android:text="@string/widget_updated_placeholder"
        android:textColor="#8D6E63"
        android:textSize="11sp" />

    <Button
        android:id="@+id/widget_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:backgroundTint="#FF9800"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:text="@string/widget_refresh"
        android:textColor="#FFFFFF"
        android:textSize="12sp" />

</RelativeLayout>
